# Ultralytics YOLO11 🚀 Self-Modal Mid-level Fusion Configuration
# YOLO11 自体多模态中期融合配置
# 策略：从RGB图像生成自体模态（边缘/纹理/梯度），在中层特征进行融合
# 适用场景：单一RGB输入，自动生成辅助模态增强检测性能

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n.yaml' will call yolo11.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # YOLO11n summary: 319 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # YOLO11s summary: 319 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512]  # YOLO11m summary: 409 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512]  # YOLO11l summary: 631 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512]  # YOLO11x summary: 631 layers, 56966176 parameters, 56966160 gradients, 196.4 GFLOPs

# 自体多模态架构定义 - RGB输入自动生成边缘模态
# 自体模态类型：self_edge（边缘检测）、self_texture（纹理提取）、self_gradient（梯度计算）
# 本配置使用边缘模态作为示例，可通过修改self_edge为self_texture或self_gradient切换模态类型

# 双路径 backbone - RGB路径和自体边缘路径独立提取特征
backbone:
  # RGB路径 (层0-7) - 提取原始RGB特征到P4级别
  - [-1, 1, Conv, [64, 3, 2], 'RGB']      # 0-P1/2 RGB路径起始（6通道输入的前3通道）
  - [-1, 1, Conv, [128, 3, 2]]            # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]     # 2
  - [-1, 1, Conv, [256, 3, 2]]            # 3-P3/8  
  - [-1, 2, C3k2, [512, False, 0.25]]     # 4
  - [-1, 1, Conv, [512, 3, 2]]            # 5-P4/16
  - [-1, 2, C3k2, [512, True]]            # 6
  - [-1, 1, SPPF, [512, 5]]               # 7 RGB特征提取结束

  # 自体边缘路径 (层8-15) - 提取自体生成的边缘特征到P4级别  
  - [-1, 1, Conv, [64, 3, 2], 'self_edge']  # 8-P1/2 自体边缘路径起始（6通道输入的后3通道）
  - [-1, 1, Conv, [128, 3, 2]]              # 9-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]       # 10
  - [-1, 1, Conv, [256, 3, 2]]              # 11-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]       # 12
  - [-1, 1, Conv, [512, 3, 2]]              # 13-P4/16  
  - [-1, 2, C3k2, [512, True]]              # 14
  - [-1, 1, SPPF, [512, 5]]                 # 15 自体边缘特征提取结束

  # 中期融合层 (层16-18) - 融合RGB和自体边缘的P4级特征
  - [[7, 15], 1, Concat, [1]]             # 16 融合RGB(层7)和自体边缘(层15)特征
  - [-1, 2, C3k2, [1024, True]]           # 17 融合特征处理
  - [-1, 2, C2PSA, [1024]]                # 18 注意力增强融合特征

# 检测头 - 基于融合特征的多尺度检测头
head:
  # P4/16分支 - 从融合特征开始
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 19 融合特征上采样 40×40 → 80×80
  - [-1, 2, C3k2, [512, False]]                  # 20 处理P4特征 (80×80)

  # P3/8分支 - 继续上采样
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 21 继续上采样 80×80 → 160×160  
  - [-1, 2, C3k2, [256, False]]                  # 22 处理P3特征 (160×160)

  # P4/16分支回连
  - [-1, 1, Conv, [256, 3, 2]]                   # 23 下采样 160×160 → 80×80
  - [[-1, 20], 1, Concat, [1]]                   # 24 融合P4特征 (80×80 + 80×80)
  - [-1, 2, C3k2, [512, False]]                  # 25 最终P4特征 (80×80)

  # P5/32分支回连  
  - [-1, 1, Conv, [512, 3, 2]]                   # 26 下采样 80×80 → 40×40
  - [[-1, 18], 1, Concat, [1]]                   # 27 融合原始融合特征 (40×40 + 40×40)
  - [-1, 2, C3k2, [1024, False]]                 # 28 最终P5特征 (40×40)

  - [[22, 25, 28], 1, Detect, [nc]]              # 29 Detect(P3, P4, P5)

# 自体多模态配置优势：
# 1. 单一输入：只需RGB图像，自动生成辅助模态
# 2. 实时生成：训练和推理时实时生成自体模态，无需额外数据
# 3. 特征增强：边缘/纹理/梯度信息增强目标检测性能
# 4. 灵活切换：通过修改self_edge为self_texture或self_gradient切换模态类型
# 5. 部署友好：无需准备额外的X模态数据，降低部署复杂度

# 支持的自体模态类型：
# - self_edge: 边缘检测模态（Sobel、Canny、Scharr算法）
# - self_texture: 纹理提取模态（LBP、Gabor、方差算法）  
# - self_gradient: 梯度计算模态（幅度、方向、组合算法）

# 使用方法：
# 1. 训练：python train.py --model yolo11n-mm-mid.yaml --data your_dataset.yaml
# 2. 推理：python predict.py --model trained_model.pt --source image.jpg
# 3. 切换模态：将第8层的'self_edge'改为'self_texture'或'self_gradient' 